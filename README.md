# AppCloud Web App 

For this web app we are using MongoDB / Express / NodeJS / React.

## Running the application
First you need to download all the packages for the server and the client using npm.
Then you can run the app using the command : npm run dev
This will launch the server and the client in development mode. 
